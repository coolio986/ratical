/** fonts.tsx — Next.js App-Router module for the configurator UI. See docs/ARCHITECTURE.md §4. */
import { Inter } from 'next/font/google';
export const inter = Inter({
	variable: '--inter-font',
	subsets: ['latin'],
});
