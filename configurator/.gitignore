# next.js dev artifacts (the production /src/build/ IS committed for on-Pi deploy)
/src/.next/
/src/out/

# Bin build outputs (ratical.mjs, pino workers, yoga.wasm) ARE committed for on-Pi deploy

# misc
.DS_Store
*.pem
.DS_Store
*:Zone.Identifier

# logs
*.log

# SciChart data (removed from OSS build; do not commit leftovers)
src/public/scichart*
# TensorFlow WASM backends are required by input-shaper fitting and MUST be committed
# (copied from @tensorflow/tfjs-backend-wasm via copy-files-from-to.json).

# Default printer config directory for development
devbox.d/printer-config/
devbox.d/klipper/

# artifacts for local deployment tests
/artifacts
